A simple file converter in Python. 
Just put the file in the Python project's folder, type the name of the file (e.g. photo.jpg). 
Then you choose the extension you want to convert the file to (e.g. png)
